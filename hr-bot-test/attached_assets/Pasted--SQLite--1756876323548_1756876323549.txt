Понял 👍
Тогда я убираю весь блок про локальный запуск и SQLite, оставляю только работу с **подключённой БД (PostgreSQL на сервере)**.

---

# Подробное ТЗ / Промпт для проекта **Sapa Edu**

## 1. Общая идея

**Sapa Edu** — это веб-приложение для обучения сотрудников.
Функции: прохождение курсов, выдача сертификатов, фишинговая проверка сотрудников, уведомления и полезные материалы.

В системе две роли: **Пользователь** и **Администратор**.
Все данные хранятся в **подключённой базе данных (PostgreSQL)**.

---

## 2. Технологический стек

* **Фронтенд**: React + TailwindCSS
  (адаптивный дизайн, плавные градиенты и анимации, минималистичный UI).
* **Бэкенд**: Python (**FastAPI** или **Flask**) + ORM (**SQLAlchemy**).
* **Хранение данных**:

  * Используется **PostgreSQL**.
  * Миграции через **Alembic**.
  * Основные таблицы:

    * `users`
    * `courses`
    * `chapters`
    * `tests`
    * `certificates`
    * `notifications`
    * `phishing_tests`

---

## 3. Функции пользователя (User)

### Регистрация / вход

* Регистрация по email + пароль.
* Вход и восстановление пароля.
* При регистрации роль по умолчанию = **user**.

### Личный кабинет

* Карточка пользователя (ФИО, email, статус, дата регистрации).
* Список доступных курсов (с прогрессом).
* Сертификаты (active, revoked, expired).
* Уведомления (`notifications`).
* Полезные ссылки (`links`).

### Прохождение курса

* Структура: введение → главы → заключение → итоговый тест.
* Последовательное прохождение (без возможности пропустить).
* При успешном тесте → сертификат (`certificates`).

### Сертификаты

* PDF с ФИО, курсом, датой, уникальным ID.
* Данные о сертификате хранятся в таблице `certificates`.

---

## 4. Функции администратора (Admin)

### Управление пользователями

* Создание/удаление пользователей.
* Сброс пароля.
* Присвоение роли (user / admin).

### Управление курсами

* Создание курса (заголовок, описание).
* Добавление глав (текст, картинки, видео).
* Итоговый тест (вопросы/ответы).
* Статус: draft → published.

### Уведомления и ссылки

* CRUD уведомлений.
* CRUD полезных ссылок.

### Фишинг-проверка

* Отправка сотруднику тестового письма.
* Уникальная ссылка `/phish/<uuid>`.
* Таблица `phishing_tests`:

  ```sql
  id | user_id | status (pending/failed/passed) | created_at
  ```
* Если пользователь перешёл по ссылке → failed → все сертификаты становятся revoked.

### Статистика

* Прогресс по курсам.
* Кол-во выданных сертификатов.
* Результаты фишинг-тестов.

---

## 5. Дизайн

* Современный, минималистичный.
* Градиенты, анимации, hover-эффекты.
* Адаптивность (мобильный/планшет/десктоп).
* ЛК = карточки + прогресс-бары.
* У админа — отдельная панель управления.

---

## 6. Минимальный рабочий прототип

1. Регистрация/вход (user/admin).
2. Создание курса (админ).
3. Прохождение курса и теста (user).
4. Генерация сертификата (PDF).
5. Фишинг-проверка и отзыв сертификата.
6. Все данные хранятся в **PostgreSQL**.

---

Хочешь, я тебе сразу **ER-диаграмму PostgreSQL** нарисую (таблицы и связи), чтобы разработчики могли быстро стартовать?
